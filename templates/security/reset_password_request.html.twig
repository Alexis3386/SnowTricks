{% extends 'base.html.twig' %}

{% block title %}Reset Password
{% endblock %}

{% block body %}
	<div class="container">
		{% for flash_error in app.flashes('verify_email_error') %}
			<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
		{% endfor %}

		<h1 class="text-center mb-3">Réinitialisation de mot de passe</h1>

		<div class="row justify-content-center">
			<div class="col-6 col-lg-4 border p-3">
				{{ form_start(requestPassForm) }}
				{{ form_row(requestPassForm.username) }}
				<button type="submit" class="btn btn-light">Ask for reset password</button>
				{{ form_end(requestPassForm) }}
			</div>
		</div>
	</div>
{% endblock %}
