{% for comment in pagination %}
    <div class="mb-3 d-flex border-bottom">
        <div style="width: 30px" class="me-3">
            <img class="img-fluid" style="max-width: 30px;" src="{{ comment.user.getpathphoto }}" alt="">
        </div>
        <div>
            <div class="d-flex align-items-center">
                <h3 class="me-2">{{ comment.user.username }}</h3>
                <small>le {{ comment.creationDate | date("d/m/Y") }} à {{ comment.creationDate | date("H:i:s") }}</small>
            </div>
            <p>{{ comment.Content }}</p>
        </div>
    </div>
{% endfor %}
