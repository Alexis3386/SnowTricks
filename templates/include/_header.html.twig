<header>
    <div class="container-fluid d-flex bg-light justify-content-between">
        <a class="navbar-brand" href="#">
            <img src="{{ asset('img/logo-monochrome-snowboarder-sautant_625815-172.avif') }}" alt="..." height="72">
        </a>
        <nav class="navbar navbar-expand-lg bg-light">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                        data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link link" href="{{ path('app_home') }}">Accueil</a></li>
                        <li class="nav-item"><a class="nav-link link" href="/#trick">Tricks</a></li>
                        {% if not app.user %}
                            <li class="nav-item"><a class="nav-link link"
                                                    href="{{ path('app_register') }}">Inscription</a>
                            </li>
                            <li class="nav-item"><a class="nav-link link" href="{{ path('app_login') }}">Connexion</a>
                            </li>
                        {% endif %}
                        {% if is_granted('ROLE_USER_VALID') %}
                            <li class="nav-item"><a class="nav-link link" href="{{ path('profil') }}">Votre profil</a>
                            </li>
                            <li class="nav-item"><a class="nav-link link" href="{{ path('app_trick_controller_new') }}">Nouvelle
                                    figure</a></li>
                            <li class="nav-item"><a class="nav-link link"
                                                    href="{{ path('app_logout') }}">Deconnexion</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
    </div>
</header>
